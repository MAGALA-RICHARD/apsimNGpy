the rust compiler to try running APSIM Models.exe via rust, just to check if speed improves.

The results
===================
it is just slightly faster than python but i believe when more than one files are provided it could improve